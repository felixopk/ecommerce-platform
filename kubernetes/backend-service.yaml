apiVersion: v1
kind: Service
metadata:
  namespace: ecommerce
  name: ecommerce-backend
  namespace: ecommerce
spec:
  selector:
    app: ecommerce-backend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
