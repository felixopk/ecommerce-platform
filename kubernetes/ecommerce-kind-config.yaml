kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 80
    hostPort: 8080      # Use 8080 instead of 80
  - containerPort: 443
    hostPort: 8443      # Use 8443 instead of 443
